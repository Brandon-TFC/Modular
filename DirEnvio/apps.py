from django.apps import AppConfig

#Clase generada automaticamente
class DirenvioConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField' #Se le agrega ya que tenemos un dato en la bd que debe ser auto increment
    name = 'DirEnvio'
