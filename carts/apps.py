from django.apps import AppConfig

#Clase generada automaticamente al momento de crear el archivo
class CartsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField' #Se añade para el auto increment de la base de datos funcione
    name = 'carts'
